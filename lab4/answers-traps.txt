1.a0-a7存放的是参数，第一个函数中13存放在a2寄存器中
2.在生成的汇编中，没有出现跳转，此时main函数对f与g的调用进行了内联优化处理。
3.首先ra先加上此时的pc值，接着与1552做运算，其结果为1552+0x30=1600=0x640,即printf函数的地址为0x640。
4.Jalr在计算出跳转地址之后会将返回地址存放在ra中，此时ra中的值为pc+4
5.程序输出是HE110 World。
57616的十六进制表示为e110，而0x00646c72，从低为到高位以此为0x72(r)，0x6c(l)，0x64(d)。
小端存储是指低地址对应低字节位，大端则正好相反，高字节位对应低地址位，
如果RISCV是大端存储，则需要要i进行修改位0x726c6400，57616不需要改变。
6.输出结果为1，y输出的结果其实应该是从a2寄存器中获取的值，不固定。